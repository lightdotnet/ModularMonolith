{
  "ExternalLoginRelay": {
    "AuthCodeTtlSeconds": 90,
    // clients/admin dev server (see clients/admin/README.md) — /login/microsoft/callback per its route.ts.
    "AllowedRedirectUris": [ "http://localhost:3000/login/microsoft/callback" ]
  }
}
