/** Mirrors Catalog.Contracts/Authorization/CatalogPermissions.cs (Categories.*) */
export const CATEGORIES_PERMISSIONS = {
  View: "catalog.categories.view",
  Manage: "catalog.categories.manage",
} as const;

/** Mirrors Catalog.Contracts/Authorization/CatalogPermissions.cs (Products.*) */
export const PRODUCTS_PERMISSIONS = {
  View: "catalog.products.view",
  Manage: "catalog.products.manage",
} as const;
