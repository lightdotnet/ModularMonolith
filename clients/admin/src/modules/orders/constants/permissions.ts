/** Mirrors Orders.Contracts/Authorization/OrdersPermissions.cs (Orders.*) — Payments.* intentionally omitted, out of scope for this pass. */
export const ORDERS_PERMISSIONS = {
  View: "orders.orders.view",
  Manage: "orders.orders.manage",
} as const;
