/** Mirrors Inventory.Contracts/Authorization/InventoryPermissions.cs (Stock.*). */
export const INVENTORY_STOCK_PERMISSIONS = {
  View: "inventory.stock.view",
  Manage: "inventory.stock.manage",
} as const;
